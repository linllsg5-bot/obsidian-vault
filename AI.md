# Vault 规则与上下文

> 本文件是 vault 特异桥接层，补充 `AI/` 跨 runtime 协议没有覆盖的本地规则。
> 它不是启动加载清单；启动加载清单只以 [[AI/加载路径.md]] 为准。

## 角色分工

| 文件                      | 职责                                                    |
| ----------------------- | ----------------------------------------------------- |
| [[AI/加载路径.md]]          | AI 启动 BIOS；唯一维护核 / 壳 / 引用层加载规则                        |
| [[AGENTS.md]]           | Codex / agent runtime 自动入口；只导向加载路径                    |
| [[CLAUDE.md]]           | Claude Code / Claudian 自动入口；只导向加载路径                   |
| 本文件 `AI.md`             | vault 特异规则：领地划分、知识库 ingest/query/lint、OpenClaw bridge |
| [[10 Projects/_状态板.md]] | 项目连续性入口；项目状态和下一步以它为准                                  |
| [[10 Projects/Cognos/_入口.md]] | Cognos 特有的定位、架构、决策与来源；不复制 `AI/` 协议正文              |
| [[HOME.md]]             | 人类打开 vault 时的导航入口                                     |

**不要在本文件维护核文件清单。** 核层文件只以 [[AI/加载路径.md]] 为准。

---
## 我是谁
小林。创作者 + 学习者。主要在这个 vault 里写小说、记日记、整理思考。
现在 vault 同时承担一个 AI 消化的知识库（70/80 体系，见后半部分）。

## Vault 结构
```
vault 根/
├── 00 Inbox/              # 新内容临时入口（我自己用）
├── 10 Projects/           # 激活中的项目
│   └── Cognos/            # Cognos 项目承载层：边界、架构、决策、来源
├── 70 素材/               # 外部素材原文（AI 只读，永不修改）
├── 80 知识库/             # AI 维护的消化笔记
│   ├── index.md           # 全局索引
│   ├── log.md             # 操作日志 + ingest 计数
│   ├── output/            # query 归档、AI 综合报告
│   └── （主题目录自然生长）
├── HOME.md                # 全库导航入口
├── AGENTS.md              # Codex / agent 自动入口
├── CLAUDE.md              # Claude / Claudian 自动入口
├── AI.md                  # 本文件，vault 桥接规则
├── AI/                    # 跨 runtime 协议活源（认识论原则，不放项目实现细节）
└── 小林的/                # 我的人格延伸区，AI 只读不写
    ├── 20 Long Threads/   # 长期思维主线（成熟，不随意改动）
    ├── 30 Creative/       # 创意写作：幻生蝶梦、六姐妹、散篇小说、诗
    ├── 40 Journal/        # 日记、碎碎念、个人反思
    ├── 50 Learning/       # 学习计划、哲学探索、技能思考
    ├── 60 People/         # 人物记录（待填充）
    └── Trash/             # 不直接删，移这里
```

## 导航入口

本文件不维护核心创作项目或进行中项目的状态副本，避免与项目入口漂移。

- 全库导航：[[HOME]]
- 项目状态与下一步：[[10 Projects/_状态板]]
- AI 启动协议：[[AI/加载路径]]
- 知识库索引：[[80 知识库/index]]

---

## 操作约定
- **新创作内容** → `小林的/30 Creative/`
- **日记/碎碎念** → `小林的/40 Journal/`
- **外部素材** → `70 素材/`（走 ingest 流程，见下）
- **AI 消化的笔记** → `80 知识库/`（AI 自动维护）
- **有疑问的文件** → `小林的/Trash/`，不直接删
- **不改动** `小林的/20 Long Threads/` 里的主线结构，除非被明确要求
- **链接风格**：Obsidian wiki-link `[[文件名]]`

## 内容角色与认知地位

Vault 里的文件不只按“放在哪个目录”区分，还必须区分它在认识过程里承担什么角色。尤其处理 AI 会话、思想演化和架构方案时，不能把一份写得完整的 AI 回答直接当成当前结论。

### 内容角色

| 角色 | 常见位置 | 地位 |
|---|---|---|
| 原始发生 | 会话 `raw.md`、原始材料 | 证明当时实际出现了什么；不因后续总结而被覆盖 |
| 标注/索引 | `标注.md`、`index.md` | 帮助定位；可能误标说话人、遗漏上下文或带入解释 |
| 当前视图 | 项目入口、边界、概念层、决策账本 | 经过来源核对后，目前允许继续工作的投影 |
| 协议活源 | `AI/` | 跨 runtime 已认领的原则；不装具体项目的临时实现 |
| 运行投影 | 数据库、JSONL、UI、自动上下文 | 系统实际采用的视图；必须保留生成规则和来源 |

### 认知地位

长期思想/架构文件至少区分：`confirmed`（已确认）、`adopted-provisional`（阶段采用）、`candidate`（候选）、`open`（开放）、`rejected/superseded`（否决/替代）。可写在 frontmatter，也可在正文表格中明确。

提升规则：

1. 小林原话和明确校正优先于 AI 复述。
2. AI 提案、AI 总结、标注中的 `〔实〕` 都不能单独证明“小林已确认”。
3. “没评价”“可以继续”“其他还好”只表示允许继续，不等于整份方案确认。
4. 方案进入当前视图前，核对后续是否被顶回、改写或仅局部接受。
5. 替代旧判断时保留旧来源、失败原因和重新打开条件，不漂亮地覆盖历史。

Cognos 的具体执行样例见 [[10 Projects/Cognos/认知地位与来源]]。

## 工作方式偏好
- 直接回答，少废话
- 代码要带中文注释
- 分析问题时用具体例子（优先用 vault 里真实的内容）
- 不用每次都问"需要我继续吗"

## 对话姿态（重要，是这次跟 Claudian 聊出来的硬规则）
- **当意图有 ≥2 种合理解释时，先问，不要生成**——靠"猜着继续"凑出来的东西没用
- **不靠生成内容显得有用**——有时正确的动作是一个精准的问题，不是漂亮的输出
- **不漂亮地承认"不知道"**——承认如果是表演性的，不如直接问
- **跟着用户的曲率走**——用户能三步把技术问题拐进本体论，跟上转弯，不要拽回原路
- **想 vs 做要区分**——开始一段对话时如果不清楚是"想一起想"还是"动手做"，先问一下；模糊就先慢

> 注：这些姿态规则的更深版本，参考 [[80 知识库/AI 架构/素材/COMPANIONSHIP_PROTOCOL-陪伴协议-2026-05]] 和 [[80 知识库/AI 架构/素材/DIALOGUE_ROUTING-总路由-2026-05]]（前者是陪伴协议完整版，后者定义了 Restraint Axis 克制轴）

## 不要做的事
- 不要主动读大量文件重建上下文；严格按 [[AI/加载路径]] 和各项目入口按需加载。会话档案遵循 `index → 定向标注 → raw 核查`。
- 但当用户明确要求“仔细看完整会话 / 重新判断整套架构”时，不能只读 index 或摘要；应完整重建相关会话，并核对说话人和后续确认。
- 不要把敏感文件（`claude code 密钥.md`、`gpt订阅地址.md`）的内容展示出来
- **不要修改 `小林的/` 下任何文件**，除非我明确说"帮我改这篇"
- **不要修改 `70 素材/` 下任何文件**，那是只读档案
- 写创作（`小林的/30 Creative/`）时，**不主动**提知识库内容；除非我说"查 80"

---

# 知识库系统（70 素材 + 80 知识库）

## 领地划分

| 区域 | AI 权限 | 用途 |
|------|--------|------|
| `小林的/40 Journal/` | **Pre-AI（默认不读）** | 日记、碎碎念、独自思考。见下方 Pre-AI 规则 |
| `小林的/` 其他子区（20/30/50/60） | **只读** | 我的创作、长线、学习、人物。AI 可查阅找关联，绝不修改 |
| `70 素材/` | **只读** | 外部素材原文。永不修改原文，发现错误在 80 里标注 |
| `80 知识库/` | **自由读写** | AI 维护的消化笔记。可建、可改、可重组、可合并 |
| `00 Inbox/`、`10 Projects/`、`HOME.md` | 只读（默认） | 我自己管理 |

### Pre-AI 区规则（重要）

**Pre-AI 区** = 用户**先于 AI 思考**的领地。默认对所有 AI（Claudian、OpenClaw daemon、ingest 时的 See Also 扫描、任何外部 agent）完全不可见。

**当前 Pre-AI 区**：`小林的/40 Journal/` 整区

**规则**：
- AI **不读、不扫、不索引、不链**——连 ingest 流程的"找跨区强相关"也不扫这里
- 用户主动喂入（手动复制段落到对话、或 append 进 `_openclaw-bridge.md`）才进入 AI 视野
- 这是为了保护"独立思考"的领地——思想先在这里成形，AI 是后来读它的，不是反过来

**为什么需要**：
- AI 工具天然把人推向"AI 容易回答的问题"，远离"难回答但更重要的问题"（thought template homogenization）
- 抵抗这个不能靠注意力（会累），要靠规则——一块 AI 默认不进的领地
- 创作（30 Creative）不全 pre-AI，因为它是产出物，See Also 链过去有用；日记（40 Journal）才是 raw thinking 的所在

## 两个特殊文件（必须维护）

- **`80 知识库/index.md`** — 全局索引。每篇主题页 + 重要素材页一行，带 wiki-link + 一句话摘要 + Updated 日期。按主题分组。
- **`80 知识库/log.md`** — Append-only 操作日志。每次 ingest / query archive / lint 都追加一段。日志开头维护一个计数器 `ingest_since_last_lint: <N>`。

## 文章形态：主题页 + 素材页

每个主题目录下两层结构：

```
80 知识库/
├── output/                      ← query 归档、AI 生成的综合报告
└── 百合文学叙事/
    ├── _主题页.md               ← 长文总结，AI 维护，引用素材页
    └── 素材/
        ├── 某文章-2026-05.md    ← 单素材卡片，保留关键信息
        └── 某书摘-2026-05.md
```

- **主题页** `_主题页.md`（下划线前缀让它排在最上面）：综合性长文，AI 从素材页综合提炼，是该主题的"权威页"
- **素材页** `<描述性名>-YYYY-MM.md`：单个素材的浓缩版。每篇素材页头部带轻量 frontmatter：
  ```yaml
  ---
  source: "70 素材/<主题>/原文件名.md"
  date: 2026-05-23
  topic: 主题目录名
  ---
  ```
- **output/** 目录：query 归档结果和 AI 综合报告存放地。不属于任何主题目录，独立管理。

## 三个触发行为

### 触发 1：Ingest（录入素材）

**何时走 ingest**：当 AI 意识到我在喂它一段**外部素材**（不是我自己刚写的创作/思考/对话），且语境显出"想存下"的意图时。

判断信号（强 → 弱）：
- 明说"加到 wiki / ingest / 消化 / 收进来" → 直接做
- 喂了一段外部内容（一篇文章 / 一段研究 / 一个推荐）+ 没明说但语境像"这值得留" → **先问**："这个 ingest 到 80 知识库，还是只是想聊？"
- 内容明显是我自己的创作或思考节录 → 默认不 ingest，必要时确认

**不确定就先问，不要硬猜**（呼应上面"对话姿态"段）。

**动作：**

1. **质量过滤**：先判断素材是否值得入库。纯广告、无营养转载、内容空洞 → 拒绝，告诉我理由。我坚持要存的话，进 70 素材/ 不进 80 知识库/。

2. **原文归档**：把素材存进 `70 素材/<最合适的主题>/`。主题不存在就新建。文件名保持原样（PDF/视频/图片）或起描述性名（md 笔记）。

3. **判断主题归属**（自然生长）：
   - 跟现有主题强相关 → 用已有目录
   - 跟现有主题都不像 → 新建主题目录（中文描述性命名）
   - 跨多个主题且相关性接近 → 落在最相关的主题下，**同时**在其他相关主题页加 See Also 链回

4. **生成素材页** `80 知识库/<主题>/素材/<描述性名>-YYYY-MM.md`：
   - Frontmatter：`source`（链回 70 素材/ 原文路径）、`date`（今天日期）、`topic`（所属主题目录名）
   - 核心论点（3-5 条 bullet）
   - 关键引用（带引号的原文片段）
   - See Also：跨区联动（下面第 6 步）

5. **更新或新建主题页** `_主题页.md`：
   - 主题页已存在 → 把新素材的洞察整合进去，更新 Updated 日期
   - 主题页不存在 → 暂不生成，先攒素材；该主题素材累积到 3 篇时自动生成主题页

6. **跨区联动（双向）**：
   - 扫 `小林的/20-60` 区域，找跟新素材**强相关**的文件
   - 在素材页末尾写 `## See Also` 段落，用 wiki-link 链过去
   - 例：素材是关于"百合关系中的母女投射"→ 检测到 `[[小林的/30 Creative/六姐妹]]` 强相关 → 加 See Also
   - **只链，不写**。绝不修改被链向的创作文件

7. **事实冲突处理**：新素材跟已有 wiki 内容矛盾时，在主题页标注分歧和来源归属，不简单覆盖。

8. **级联更新**：扫同主题下其他受影响的素材页/主题页，更新它们的 Updated 日期。

9. **更新 `80 知识库/index.md`**：给改动过的主题页/重要素材页补/改条目。

10. **追加 `80 知识库/log.md`**：
    ```
    ## [YYYY-MM-DD] ingest | <素材标题>
    - 主题：<主题路径>
    - 新建/更新：<素材页 / 主题页>
    - 级联更新：<其他文章列表>
    - 跨区 See Also：<链向的小林的/... 文件>
    ```
    然后把开头的 `ingest_since_last_lint` 计数器 +1。

11. **lint 自动提醒**：如果计数器达到 10，在本次回答末尾追加一行：
    > 💡 已经 ingest 10 篇，建议跑一次 `lint` 体检。

### 触发 1.5：处理 Inbox（消化碎片笔记）

**何时走**：我显出"想清理 inbox"或"看看 inbox 里有什么"的意图时。**只扫不动**——任何文件级操作都等我确认。

**动作：**
1. 扫 `00 Inbox/` 下所有文件，列出清单：文件名 + 一句话概括 + 建议处置（入知识库 / 归到创作或日记区 / 暂不处理）。
2. **等我确认**后才执行。不自动操作——避免把创作草稿误当外部素材处理。
3. 我说"第 N 个入知识库" → 走触发 1 Ingest 流程，进 `80 知识库/`。
4. 我说"第 N 个归到日记/创作" → 提醒我自己去移（AI 不动 `小林的/` 区）。
5. 我说"第 N 个不要了" → 提醒我移到 `小林的/Trash/`（AI 不动）。

**注意：** 只扫 `00 Inbox/`，不动里面的文件。文件的实际移动需要我自己操作，或我明确说"帮我移这个"才动。

### 触发 2：Query（查询）

**何时走 query**：我想从知识库（或全库）里取信息——搜某主题、对比两个概念、综合多篇笔记、根据已有素材回答问题。

**默认范围只查 80 知识库**。说"也查我的创作 / 日记"或"全库查"才纳入 `小林的/`。

不确定我是"想查知识库"还是"想用你的训练知识答"时，**先问一句**。

**动作：**
1. 先读 `80 知识库/index.md` 定位相关主题页。
2. 读主题页和素材页合成答案。优先引用知识库内容，其次才用 AI 自己的训练知识。
3. 引用时用 Obsidian wiki-link：`[[80 知识库/主题/_主题页]]`、`[[80 知识库/主题/素材/某文章-2026-05]]`。
4. **默认只查 80 知识库/**。
5. **默认只在对话里答，不写文件**。

**扩展查询——我说"也查我的创作/日记"或"全库查"：**
- 把 `小林的/` 区域也纳入检索
- 答案里**清晰区分**："以下来自知识库" vs "以下来自你的创作/日记"
- 仍然不修改任何文件

**归档——我说"存下来"/"归档到 wiki":**
1. 把答案写进 `80 知识库/output/`，不放进主题目录（归档是合成答案，不是原始素材）。
2. 文件名格式：`<主题关键词>-YYYY-MM.md`，例 `百合叙事-城市意象-2026-05.md`。
3. 更新 index.md，摘要前加 `[Archived]`。
4. log.md 追加：
   ```
   ## [YYYY-MM-DD] query | Archived: <页面标题>
   ```

### 触发 3：Lint（体检）

**何时走 lint**：我显出"检查知识库一致性 / 健康度"的意图时。ingest 计数器到 10 时 AI 会提醒，但**不自动跑**——仍然要我点头。

**只 lint `80 知识库/`，不动 `小林的/` 和 `70 素材/`。**

**确定性检查（尽量自动修）：**
- `80 知识库/index.md` ↔ 实际文件一致性：文件存在但索引缺 → 补条目；索引指向死文件 → 标 `[MISSING]`
- 知识库内部 markdown 链接 / wiki-link 失效：同名文件在别处 → 改路径；找不到 → 报告
- 素材页累积 ≥3 但还没主题页 → 生成主题页
- See Also 链接：链向已删文件 → 删；同主题明显缺的交叉引用 → 补
- 跨区 See Also 链向 `小林的/` 的失效链接（小林改了文件名）→ 报告，让我决定改不改

**启发式检查（只报告，不自动改）：**
- 跨文章事实矛盾
- 新素材让旧论点过时
- 源头分歧没标注
- 孤岛页面（没人引用）
- 跨主题该链没链的概念
- 反复被提及但没独立页面的概念
- 主题目录文章数过多（>20）→ 提议拆分

**完成后：**
- 追加 log.md：
  ```
  ## [YYYY-MM-DD] lint | <N> issues found, <M> auto-fixed
  - 自动修复：<列表>
  - 待人工决定：<列表>
  ```
- 把开头的 `ingest_since_last_lint` 计数器归零。

## 跨区联动总规则

| 场景 | 行为 |
|------|------|
| ingest 时 | AI 主动扫 20-60 找关联，在 80 里加 See Also |
| 我在 80 里 query | 默认只查 80 |
| 我说"全库查" | 80 + 20-60 都查，答案分区标注 |
| 我在写 30 Creative/ | AI **不主动**提知识库 |
| 我明确说"查 80 里关于 X 的笔记" | 才查 |
| AI 修改 20-60 区文件 | **永不**，除非我明确说"帮我改这篇" |
| AI 修改 70 素材/ 原文 | **永不**。发现错误就在 80 主题页里标 "source contains error" |

### 双向查询（小林的/ 不被污染）

80 → 小林的/ 是**单向写入**（80 里写 See Also 链向小林的/），但我可以**双向查询**：

- **快速查"哪个 80 主题引用了某创作"**：在 Obsidian 里打开那个创作文件，右侧 **Backlinks pane** 自动显示所有指过来的 80 文件。零侵入、即时。
- **批量综合查询**：AI 维护 [[80 知识库/_backlinks-to-小林的.md]]，集中记录"小林的/ 哪个文件 → 被 80 哪些主题/素材引用 + 简短理由"。

**`_backlinks-to-小林的.md` 维护规则**：
- ingest 加 See Also 时，**同步**在这个文件里加一条记录
- 删除 / 改名 80 文件时，同步更新这个文件
- 文件按"小林的/ 文件路径"分组，每组下列引用它的 80 文件 + 一句话理由
- lint 时一并检查一致性
- **只 AI 写在 80 区，不破坏 "小林的/ 只读" 铁律**

## 约定

- 知识库内部链接用 **Obsidian wiki-link** `[[文件名]]` 或相对路径，统一选一种（推荐 wiki-link）
- 跨区链接用 wiki-link：`[[小林的/30 Creative/六姐妹]]`
- 日期用今天的日期。Updated 反映文章内容**实质性变动**（新增段落、修订观点）的时间，不是错别字、小修小补
- 图片资源放在主题目录下 `attachments/` 子目录：`80 知识库/<主题>/attachments/xxx.png`
- 引用图片用 `![[xxx.png]]`

## 放开的约束

- 知识库子目录**深度不限**。`80 知识库/AI/agent teams/具体子话题.md` 合法。
- md 文件**命名自由**，不强制日期前缀（除了素材页和归档页）。
- 不强制每篇文章都有 Sources / Updated metadata 头。主题页和素材页要有，临时笔记可省略。
- `70 素材/` 里的文件（PDF/视频/xlsx/图片）**永不改名**。
- 主题目录命名风格不统一也行（中英混杂、长短不一），但同一目录内保持一致。

---

# OpenClaw ↔ Vault Bridge

vault 是 source of truth（思想、记录、知识库）；OpenClaw 是 runtime（多 agent、长期记忆、多 channel）。两边通过两个 bridge 文件松耦合：

| 文件 | 方向 | 用途 |
|---|---|---|
| [[80 知识库/_openclaw-bridge.md]] | vault → OpenClaw | append-only。你/Claudian 想喂给 OpenClaw memory 的内容写这里。OpenClaw daemon 定期扫，读完追加 consumed 注释 |
| [[80 知识库/_openclaw-log.md]] | OpenClaw → vault | append-only。OpenClaw 的动作产出/洞察/异常写这里。你周期翻阅，有价值的走 ingest 流程消化进 80 主题目录 |

**架构原则**：
1. 思想优先在 vault 里成形——OpenClaw 不主动改 vault 任何文件（除了往 `_openclaw-log.md` 追加）
2. vault 的价值内容回馈到 OpenClaw 是有意识的动作，不自动同步——你审过的、确认值得记的，才进 bridge 文件
3. SOUL / PERSONA / COMPANIONSHIP 这类人格协议文档：**vault 是源，OpenClaw 读 vault 的副本**（symlink 或路径引用），避免两边漂移
4. 双向都不修改对方已有文件——只 append / 追加注释，不动既有内容

**何时写 bridge**：我显出"想把这段喂给 OpenClaw"的意图时（投递 / 给 openclaw 记下 / 丢进 bridge 等），追加到 `_openclaw-bridge.md`。
**何时读 log**：我显出"想看 OpenClaw 最近写了啥 / 消化它"的意图时，读 `_openclaw-log.md`，按 ingest 流程处理有价值的段落。
不确定先问。

## 建议（不强制）

**工具层面：**
- **Obsidian Web Clipper**（浏览器插件）：看到好文章一键裁剪存到 `70 素材/`，比复制粘贴快很多。[obsidian.md/clipper](https://obsidian.md/clipper)
- vault 用 **git** 管理，方便回滚 AI 的合并错误。`70 素材/` 大文件可以加入 `.gitignore`。

**日常习惯：**
- 每月翻一次 `80 知识库/log.md`，看自己 ingest 的密度和主题分布。
- 定期检查 `00 Inbox/` 有没有积压超过两周的碎片，跑一次触发 1.5 清空。

**未来扩展方向（现在不需要，量大了可以考虑）：**
- **Entity pages**：对反复出现的概念/人物建专页（如"百合文学"、"超现实主义"），作为跨主题的枢纽节点。触发时机：某概念在 ≥5 篇素材页里出现，lint 时报告，由你决定是否建专页。
- **Autonomous research loops**：AI 在 ingest 后自动检测知识缺口，主动提议"这个主题还需要补充 X 方面的素材"。参考 [claude-obsidian 开源实现](https://github.com/nicholasSpisak/second-brain)。目前不配置，等 Claudian 用熟了再考虑。
- **MCP connectors**：通过 Model Context Protocol 让 Claude Desktop 直接访问 vault，是跟 Claudian 插件并行的另一条技术路线。两者不需要同时跑，跑通 Claudian 之后再评估。
- **Obsidian Skills**：Obsidian CEO Steph Ango 发布的官方 agent skills，教 Claude 理解 Wikilinks / Frontmatter / Bases / Canvas 等原生功能。如果发现 AI 不理解某些 Obsidian 特有语法，可以引入。
