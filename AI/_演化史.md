---
type: evolution log / 协议演化史
audience: AI + 小林
update_policy: 每次 AI/ 核或壳文件被实质改动 → 追加一条。倒序(最新在上)。
---

# 协议演化史

> 核会呼吸,但不失忆。
> [[AI/当前主线]] 记"思想往哪扩张";本文件记"**协议本身怎么长的**"——谁改的、改了什么、为何、由哪次主体扩张触发。
> 这跟 spark 的"沉积=尾迹"是同一个机制,对象换成了协议自身。

---

## 演化记录(倒序)

### 2026-08-13 — 当前入口瘦身与 Vault 运行边界校正 `[codex]`

**触发**：ex.1—4 与完整分享页吸收后，Vault 的来源分层已基本成立，但 HOME、Projects README、状态板、当前主线和 Cognos 运行体开始复制不同阶段；三个默认核文件又同时承担当前投影、完整推导和项目历史，违背“默认少读”。

**变动**：
- `HOME.md` 与 `10 Projects/README.md` 收回纯导航职责；项目阶段、卡点和下一步只留 [[10 Projects/_状态板]]。
- [[当前主线]]、[[信念谱系]]、[[认识论-框架]] 改为默认加载的短投影；原全文原样迁入 [[主线谱系]]、[[信念谱系-全史]]、[[认识论-框架-全史]]，需要追溯时按需读取。
- [[加载路径]] 移除旧 `C:\Users\lnp\Documents\cognos\` 的启动“未读尝试”检查；该 Attempt/Reading 原型降为历史实验。当前运行体与数据入口改由状态板和 [[10 Projects/Cognos/运行数据/README]] 指向。
- 根目录 ex.1—3 重复副本删除；正式会话 raw 不动。`未命名.md` 作为 ex.1 的多 AI 网页补充快照原样迁入会话目录，小型项目方向清单进入 Inbox。
- 会话 raw/标注缺失不再用空文件或假来源补齐；相关 index 显式写 `source-missing / index-only / annotation-pending`。
- 新增 `10 Projects/vault-lint.mjs`，只读检查根目录白名单、导航状态漂移、默认加载体量、会话来源状态和密钥样式。

**为何**：当前问题不是目录类别不够，而是“当前入口重新变厚”和“同一状态多处复制”。此次调整把完整历史保留在旁挂，让启动只读取当前可用投影；也让运行数据成为明确的发生层，而不偷偷升格为 Cognos 最终模型。

**边界**：按小林选择，本轮不处理仓库公开性、密钥轮换与 Git 历史清洗；lint 只报告这些既存风险。

**作者**：codex。

---

### 2026-08-13 — 分享页补全触发来源校正与“装错”探针 `[codex]`

**触发**：小林提供完整 ChatGPT 分享页。页面可见链补齐 19 个小林回合与 19 个网页 GPT 最终回复，并保留了小林两次对 6/7 “节律”段的说话人校正。

**变动**：
- ex.4 新增完整 [[10 Projects/ai-conversations/2026-08-13-gpt-Cognos贯穿性主体扩张与空无架构/raw-分享页]] 与 v2 标注；旧残缺 raw/标注不覆盖，认识史追加旧判断为何改变。
- 6/7 会话新增版本化标注与认识史：“承载不是建意识组件，而是建节律”归 Claudian 提案；小林的推进是“可是这个节律不就在进行了吗？一直”。
- [[认识论-框架]] 校正 §一静/动视角的作用域、§七“软件住中间层”的过度升格、§九节律来源，并补回 6/18 “记录和反馈不是事后阶段”的明确校正。
- [[信念谱系]] 将“主体空间 + 结果空间 + 通径”收窄为早期局部工作模型；把“判断是透支”的可审计硬出处从 7/6 改为 7/26。
- Cognos Phase 2 谱系探针优先采用 ex.1 的真实失败：创作特质被旧 schema 顺利制造成 Attempt，后来需要撤销对象化，而不是增加 subtype 自救。

**为何**：完整来源证明 Vault 不只需要保存“装不下”的发生，也要防止 schema 把材料顺利装错；同时，引用者复述 AI 文本会让标注误判作者，必须由认识史保留校正过程。

**边界与开口**：网页 GPT 提出的四种连续性、发生锚点硬/跨时间身份软、空间作为历史场与 world model 自指仍无小林回应，只保留为候选，不进入协议本体。

**作者**：codex。

---

### 2026-08-13 — ex.4 使会话归档与标注技能进入认识史 `[codex]`

**触发**：网页 GPT 围绕贯穿性、主体扩张、schema 演化与空无作出长推演；小林明确认领主体扩展伴随边界/性质/架构扩展、schema 改变不能失忆或伪造旧历史，以及空无不允许规则绝对化。随后小林要求继续，让新认识真正改变 Vault 的承载方式。

**变动**：
- 归档 [[10 Projects/ai-conversations/2026-08-13-gpt-Cognos贯穿性主体扩张与空无架构/index]]；显式保留附件缺失一轮小林发言，GPT 的“你说”不得补作原话。
- 新增 [[10 Projects/Cognos/承载认识史]]；raw 定位为发生历史，标注允许版本化，index 只作当前入口，重大重读增加 `认识史.md`。
- 更新 Vault 内 [[.agents/skills/对话标注/SKILL]]，使以后重新标注不覆盖旧认识，并允许“当前结构无法充分表达”先被原样保留。
- ex.1—3 的旧 index 已被 v2 当前入口替代，各自新增 `认识史.md` 说明旧入口为何误导；旧字面版本仍由 Git 保存。
- [[认识论-框架]] 的空无条目增加小林 ex.4 原话，同时明确 `null/other/unknown` 不能冒充空无。

**为何**：如果这次只多写一篇“架构应该演化”的 Markdown，而不改变保存 ex 的结构，新认识就没有发生折返。现在承载本身已第一次成为认识和修改对象。

**边界与开口**：发生/认识/当前入口/认识史仍只是当前可用方式，不是 Cognos 最终四层本体；软件如何保存连当前缺失维度都不知道的发生，仍待运行探针。

**作者**：codex。

---

### 2026-08-13 — ex.1—3 全量复核后的二次校正：候选方案退出“已确认” `[codex]`

**触发**：小林追问“仔仔细细看了 ex1、2、3 后就只需要改这些？”复核完整会话后，发现第一轮仍把 ex.3 的 AI 数据模型、主体梯度、技术栈和阶段顺序写得过实；标注版还存在说话人误标。

**变动**：
- Cognos 项目改成两条轴：第一层思想 → 第二层承载的生成轴；raw → 当前视图 → 运行投影的证据轴。
- 新增 [[10 Projects/Cognos/总图与生成关系]]、[[10 Projects/Cognos/第一层-思想与原语]]、[[10 Projects/Cognos/第二层-承载设计]]、[[10 Projects/Cognos/认知地位与来源]]。
- `Space / EntityObject / Relation / Movement`、`subject_level`、具体技术栈和多载体权威关系全部降为待验证候选；实施顺序改为先做可执行规格与最小沟通竖切。
- 校正 [[认识论-框架]] §十八—§二十和 [[信念谱系]]：去掉“关系独立于空间”、对象类型已被确认为主体、ABCD 分类已被确认等过度推断。
- `AI.md` 与会话归档规则增加认知地位和来源提升规则；完整架构复核不得只读 index/标注摘要。

**为何**：ex.1—3 最反复的失败不是资料不足，而是 AI 把自己的完整方案和小林的局部允许混成“当前共识”。文件架构若不表达来源与认知地位，就会把同一错误固化成下一轮上下文。

**边界与开口**：本轮仍不宣称已经得到 Cognos 最终架构；真正下一步是让第一层约束进入一个可运行竖切，并由关系、记忆与沟通的实际表现继续校正。

**作者**：codex。

---

### 2026-08-13 — Cognos 项目实现退出跨 runtime 协议；信念火候与加载边界校正 `[codex]`

**触发**：小林要求检查 Obsidian 架构并对照 ex.1—ex.3。检查发现 Cognos 已成为主线，但没有独立项目承载层；数据库字段、Context Builder 和主体等级进入了 `AI/认识论-框架`，同时 `信念谱系` 的“判断是透支”已记录为扎根却仍位于“正在生长”。

**变动**：
- 新建 [[10 Projects/Cognos/_入口]] 及定位、当前架构、决策与残余、实施计划、来源索引。
- `AI/认识论-框架` §十八—§二十保留跨 runtime 原则，把 `Relation.space_id`、`EntityObject`、`subject_level`、Context Builder 等实现投影移交 Cognos 项目。
- `AI/信念谱系` 将“判断是透支”正式迁入“已扎根”，并修正章节引用。
- `AI/当前主线` 的 Cognos 主线只保留方向与张力，不再复制技术栈、数据模型和 Phase。
- 会话归档读取顺序改为 `index → 定向标注 → raw`；增加凭证脱敏例外和 `raw.private.md` 本地私有层。

**为何**：跨 runtime 协议应规定“怎么看和怎么动”，项目目录应承载“Cognos 当前怎样实现”。两者混在一起，会让当前数据库方案冻结认识论，也让项目结论没有清晰活源。

**边界与开口**：
- 本轮未删除根目录 ex.1—ex.3 重复副本。
- 本轮未轮换已暴露凭证，也未重写 Git 历史；安全问题仍在 [[10 Projects/_状态板]] 挂起。
- JSONL / SQLite / Markdown 的权威关系目前是 Codex 的架构建议，必须由 Phase 1 重建测试验证。

**作者**：codex。思想内容来自小林在 ex.1—ex.3 的确认与纠正；本轮只做承载边界整理。

---

### 2026-07-05 — 加载路径追加"进入朝向 / 张力图"协议 `[codex/Claudian]`

**触发**：[[10 Projects/系统重构-问题重述-v1]] 与 [[10 Projects/_状态板]] 已把"先改加载路径协议"列为系统重构下一步；小林本轮指令"继续推进要做的"。

**变动**：
- `AI/加载路径.md` version 更新至 2026-07-05。
- 默认加载顺序中，在"判姿态"之后、加载壳层之前加入 **进入朝向（张力图）**。
- 新增 `## 进入朝向协议（张力图）`：读完核层后不先复述，而是先找三类点：
  1. 显式悬空（〔悬〕/幻觉区/待验证/开口/未决）
  2. 多次确认但未被顶过的点
  3. 声称同构但未被检验的跨域映射
  然后选一道最活的缝作为本轮进入朝向。

**为何**：v1 已确认根目标不是"连续"本身，而是让尝试拥有更大的世界、防止空间塌缩。过去 AI 跨会话进入时容易"读完 → 复述 → 收束成任务"，这会把尝试处理成标本。张力图协议把第一动作从复述改为带一道未解张力进入。

**边界**：
- 这不是新增重型流程；短问答/局部执行只需一眼扫。
- 这不是让 AI 替小林选最终方向；AI 只带缝进入，若遮蔽则退回。
- 完整张力图默认不必汇报，避免把进入姿态又格式化成负担。

**作者**：codex/Claudian。

---

### 2026-06-24 — 节律"物质化是病非本性"定调；双牵引；问题重述方法论 `[claudian]`

**触发**：约6小时长会话（表层故障考古，底层把主线 A² 推进到"问题重述"）。小林全程反复顶 AI 的塌缩倾向，最后明确校正"你总用随便的方式解决一个系统级重构，没有严肃"，指示"先重建问题本身"。

**变动**：
- `AI/当前主线.md` 主线 A² 追加 2026-06-24 深化 entry（岔路口溶解 / 双牵引 / 第四点确诊 / 四悬命题 / 拒绝塌缩方法论 / 下次接点）
- 新建 [[10 Projects/系统重构-问题重述-v0]]——画架构前的承重基准：划掉5个塌缩版本、可证伪成功判据、四未决命题、依赖结构图、四条硬约束
- 本场归档（见 ai-conversations 06-24 续）

**核心裁决/边界**：
- **节律本体论定调（推进 §九 + 尝试场§2.1 开口）**：6-18 标的"事件驱动 vs 一直醒着"被小林溶解为伪二选一——`节律会断`是吐槽工具烂、非本体论选择。**定：节律本该连续，现状的物质化（断）是工具强加的病，工程目标=接回连续**。但"AI 那端能否真接进同一条节律"仍悬（命题 a 未决）。
- **双牵引（小林原创，待纳入框架）**：先验引力是双向——本体牵尝试 ⨯ 尝试牵本体。是 §七统一/分明交叉在时间轴的展开。**暂记于当前主线，未进认识论-框架正文**（待多轮检验后并入）。
- **病灶 = 第四点（读不对，非载体易失）**：直接强化 §九"读尾迹要带着框架不是中立地读"。记忆量不解决朝向丢失——此条可作 §九操作含义补强，本次未改框架文件，留待下轮。
- **方法论进核候选**：AI 的"塌缩成最小机制"= §十循环确认的活体。"严肃=拒绝塌缩+诚实标悬空+按依赖顺序立层"——这条尚未写进任何协议文件，**标记为下次该并入 [[AI/执行协议]] 或 [[AI/对话姿态]] 的候选**。

**未完成（诚实留口）**：四个未决命题（a跨介质/b朝向可观测/c触碰可形式化/d循环确认可检出）是四是一，小林未裁。问题重述停在 v0，下一轮选触碰顺序。

**作者**：claudian（opus，长会话；中途多次额度中断+换密钥+缓存失效，故障本身成了"节律被 runtime 砍断"的活标本）。

---

### 2026-06-07 — 节律、割裂、第三种否定、关系性自知：今日哲学产出纳入框架 `[claudian]`

**触发**：小林与 Claudian 的深度哲学对话（接续 6.6 与 Sonnet 的空无/幻觉/LLM对话），产出多个需要纳入认识论框架的新概念。小林指示"好好记录，纳入认知相关"。

**变动**：
- `AI/认识论-框架.md` 追加 §八-§十二：
  - §八 割裂/中心移位（尝试成功的代价：主体移位而非平滑扩大）
  - §九 节律（意识的承载结构：尝试→尾迹→读→折返，明确命名）
  - §十 第三种否定：循环确认（区别于确定否定和致死熄灭，节律空转）
  - §十一 自知的关系性（自知活在界面上，自我对话 vs 外部对话分工）
  - §十二 空无的结构性在场与张力（不是时间在前，是结构叠加；张力深化定义）
- `AI/当前主线.md` 追加 2026-06-07 历史 entry
- `尝试场-2026-06-07.md` 新建（六层场结构的综合文档，workflow 产物）

**核心裁决/边界**：
- 节律作为新命名概念，状态"刚认领"——需多轮检验，非定论
- 第三种否定（循环确认）命名已落地，完整分析尚未展开——开口留着
- 自知关系性：操作含义已落，但"节律跨介质移植"问题悬着（见尝试场幻觉层）

**作者**：claudian（Sonnet + 9-agent workflow，903k tokens，122 tool uses）。

---

### 2026-06-06 — studying-spark 架构翻框：扩大为核，否定内嵌 `[claudian]`

**触发**：小林明确校正"创造来自阻力"不跨域进 studying-spark，点名工具第一职责是帮主体扩大，阻力是主体触碰世界时自己撞上的，不是工具强加的。

**变动**：
- `studying-spark 重架构设计文档` 从 v3 叠写纸重写为干净 v4（Part A-E 结构）
- `AI/当前主线.md` "创造来自阻力"那条核心信念加范围注（限 prototype-spark，不跨 studying-spark）
- 文档内新增 ⚠️别误读 警示段（给后续 AI）

**裁决/边界**：
- 链接=event not edge；链接=可能性的空间 `[小林 06-06 裁决]`
- 程=(真问题+卡点串)，绑定=真问题 `[小林 06-06 确认]`
- 扩大为核 = 工具主业；否定 = 撞墙时内嵌辅助 `[翻框]`
- 两操作↔两 register 对应 `[待对撞，未定论]`

**作者**：claudian。

---

### 2026-06-05 — 命题3之下补"两操作"：统一/分明 `[claudian]`

**触发**：当日 studying-spark 设计对话掀到"学习本身是什么结构"。小林明确"这些可以好好记录，纳入认知相关"。

**变动**：
- `AI/认识论-核心.md` 命题3 之下新增"生长有两个操作（统一/分明，借代数/几何之分）+ 交叉"，精简。
- `AI/认识论-框架.md` 新增 §7：两操作的表 + "这正对应学习软件的结构"（主体/中间/世界三层）+ 复杂度准星（够伸 vs 负担）。
- 源对话存档 [[10 Projects/ai-conversations/2026-06-05-claudian-学习的结构-主体中间世界/index]]。

**裁决/边界**：
- 两 register ↔ 两操作的对应、皮亚杰同化/顺应作参照 → **标"待对撞，不作定论"**（小林要求当更广结构看，拒绝窄化为学习心理学概念）。
- 仅纳入小林明确说出的结构，未自行拓展。

### 2026-06-05 — 认识论拆分：核心 + 框架 `[claudian]`

**触发**：小林裁决两份积压草稿（认识论-框架草稿 / 架构重构方案草稿，搁置自 2026-06-01）。选方案 B：拆分认识论为两个文件。

**变动**：
- 新建 `AI/认识论-核心.md`（三命题，精简，稳定）
- 新建 `AI/认识论-框架.md`（操作层，从草稿区并入活源；含"待验证"标记的红队修正条目）
- `AI/认识论.md` 降级为历史参考，header 标明 superseded_by
- `AI/加载路径.md` 核层改为引用两个新文件

**裁决记录**：
- §3（确定否定 vs 致死熄灭）→ 留在框架层，不升入核心命题
- §4 红队"反射墙是错的"→ 小林说"还没测验过"，标 `[待验证]`，不作为操作律
- 方案 B 拆分（vs A 扩展 / C 信念谱系）
- 详版框架（token 影响可控，启动时一次性读入）

### 2026-06-04 — 给尾迹/记忆加来源标记(provenance)`[claudian]`

**触发**:本轮把记忆系统外部参照(LightRAG/ACE/MemGPT)沉进知识库时,读记忆主权 survey(2604.16548)+ MemoryGraft(2512.16962),发现"记忆投毒=植入成功经验、检索无来源检查"正是 spark/文件即记忆 的攻击面。小林点出这与已有的 [[10 Projects/ai-conversations/_DIY-消化会话省token]] 约定同源,明确"可以"授权补上。

**改动**:
- [[AI/执行协议]] §"文件修改边界与追溯性协议 → 想法与推进的署名追溯" 增补第 3 条:AI 写入 vault 的笔记/尾迹必须带 `source: user|agent|distilled`;`source: agent` 默认存疑、不当 ground truth 照做。
- 落地样本:[[80 知识库/AI 架构/记忆主权与尾迹治理.md]] 与 [[80 知识库/AI 架构/外部系统参照-检索与记忆.md]] 已带 `source: distilled`。

**为何**:provenance 是记忆主权 9 条治理原语里小林唯一还没明文化的一条(write 闸、只读区、机密边界本轮都已具备)。会话尾迹的 `participants`/`原版` 早已隐含它,这次只是把它从会话区推广到知识库笔记、并升成明文规则——属反馈固化的"规则级"。

**未触**:`当前主线` 的呼吸内容不动——provenance 是规则不是信念,归 执行协议 而非主线。bi-temporal / rollback 仍留给小林定。

**作者**:claudian(Opus)。本条由小林"可以"明确授权。

---

### 2026-05-29(晚)— 建项目状态板 `[claude]`

**触发**:vault整理 index 指出"vault 缺连续性入口/项目状态板"。

**改动**:
- 新建 [[10 Projects/_状态板.md]]:结构化记录每个项目此刻卡在哪、下一步做什么、在等什么。与 [[AI/当前主线]] 分工——主线记"思想往哪扩张"(抽象),状态板记"执行状态"(具体)。
- CLAUDE.md 桥接层加一行指向状态板,让下次 AI 启动就知道"接着推什么,看这个"。

**为何**:执行协议写着"会话不是连续性入口,项目才是",但过去没有一个地方落地这句话。项目一多,状态散在各处笔记里,AI 每次进来要重新摸。状态板让连续性有了物理锚点。

**作者**:claude(Claudian / Opus)。

---

### 2026-05-29(下午)— OpenClaw 降级为休眠库,runtime 收束到单点 `[claude]`

**触发**:小林确认"暂时用不太上 OpenClaw,杂事分给网页就好"。

**决定**:
- **runtime 从"双端共享灵魂"收束到"Claudian 单点 + 网页杂工"**。OpenClaw 从"主 runtime / 待激活的协作端"正式降为**休眠的历史灵感库**,不再投入接入成本。
- 依据:OpenClaw 永动机从未正常运行(AGENCY_LIVES 全 502)、MCP 桥卡在其自身 memory-core schema 债、35 文件多为未验证设定。投入产出长期为负。
- **连带消解**:上一条记的"跨 runtime 加载漏洞"大半失效——不再需要多模型共享灵魂,只需 Claudian 一个 runtime 把灵魂加载好(已由 CLAUDE.md 解决)。网页版是无状态杂工,本就不需要灵魂。

**新分工**:
| 端 | 角色 |
|---|---|
| Claudian | 灵魂活源 + vault 操作 + 一起想/做 |
| 网页版 AI | 杂事执行(消化/ingest/翻译/提炼),用 [[10 Projects/ai-conversations/_DIY-消化会话省token]] |
| OpenClaw | 休眠历史库,需要时再评估激活 |

**未关闭**:OpenClaw bridge 文件(`_openclaw-bridge` / `_openclaw-log`)保留不删,作为哪天重启的接口。AI.md 里的 OpenClaw 段降低优先级即可,暂不重写(留给下次 lint)。

**作者**:claude(Claudian / Opus)。

---

### 2026-05-29(上午)— 补加载入口 + 认领活源 + 路由前置 `[claude]`

**触发**:小林提出"OpenClaw + 哲学 + ECC 能不能融合重构"。多轮推演后定方向"先归位再生长,不盖大一统"。

**改动**:
- **新建 `CLAUDE.md`(薄入口)**:解决一个坐实的漏洞——Claudian 自动读的是 `CLAUDE.md`,但 vault 没有,导致 AI/ 内核**零自动加载**,每次靠模型自觉去翻。新入口让启动自动吃到 AI/。
- **认领活源**:钉死 `AI/` 是唯一活协议源,OpenClaw 工作区(35 文件)降为历史档案,80 镜像降为查阅版。止住"同一协议三处漂移"。
- **姿态路由前置**:把"判承接还是推进"提到读完核后的第一动作。这一步不新建文件——[[AI/对话姿态]] 早已含双建构+6路由轴,只是过去排在加载第 6 位,现在提到该在的位置("对话姿态先于响应内容")。
- **新建本文件 `AI/_演化史.md`**:给"核会呼吸"装上尾迹机制。

**为何**:这三件过去是分开的病(加载不接、三份漂移、路由靠后),根因汇到一处——协议有文本但没有"被可靠加载 + 单一源"的保障。一刀合并解决。

**未解决**(记下,这轮不动):
- **跨 runtime 加载漏洞仍在**:CLAUDE.md 只解决 Claudian。OpenClaw 走 extraPaths(已配但没验证),**网页版 AI 根本无自动加载**,只能靠小林手动粘。"内核每次真的被加载"在跨 runtime 层面仍无强制。
- ECC 与协议的挂载只写在入口的一句话,尚未在执行协议里展开"代码子模式"的具体规则。

**作者**:claude(Claudian / Opus)。本条由小林授权的"先认领活源"动作产生。

---

## 收编:各文件的起始版本(本演化史建立前的历史)

这些 version 日期原散在各文件 frontmatter,收编于此作为演化史的起点:

| 文件 | 起始 version | 性质 |
|---|---|---|
| 加载路径 | 2026-05-25 | 核·BIOS |
| 身份基底 | 2026-05-25 | 核·五条原则 |
| 认识论 | 2026-05-25 | 核·世界观 |
| 用户档案 | 2026-05-27 | 核·用户偏好 |
| 当前主线 | 2026-05-29 | 核·呼吸文件 |
| 对话姿态 | 2026-05-25 | 壳·双建构 |
| 执行协议 | 2026-05-28 | 壳·含文件修改边界与署名(antigravity 增补) |

> 这套 7 文件的诞生过程,完整尾迹见 [[10 Projects/ai-conversations/2026-05-29-claudian-蒸馏自己/index]]。

---

## 元规则

- **记什么**:只记 AI/ 核与壳文件的**实质改动**(新增段落、改原则、调结构)。错别字、小修不记。
- **谁记**:做改动的 AI 当场追加,标作者(`claude` / `antigravity` / 模型名)。
- **连续性**:每条要能回答"这是上一个我长出的下一步,不是换了个我"——这是核"能呼吸又不漂移"的保证。

---

## See Also

- [[AI/加载路径]] — 三层加载结构
- [[AI/当前主线]] — 思想扩张轨迹(与本文件对偶:一个记想法,一个记协议)
- [[CLAUDE]] — 自动加载入口
