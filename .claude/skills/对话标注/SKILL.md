---
name: 对话标注
description: 把 AI 对话原文整理成可追溯的发生/认识/当前入口归档，并在后续重读时保留旧解释与认识变化。当小林说整理、标注、归档或重新解读一场会话时触发。
---

# Claude 兼容入口

本文件只负责让仍从 `.claude/skills/` 发现技能的 runtime 进入跨 runtime 活源。

执行任何动作前，完整读取：

`../../../.agents/skills/对话标注/SKILL.md`

并严格按其当前规则执行。不要在本文件复制或维护标注流程；唯一活源是 `.agents/skills/对话标注/SKILL.md`。

